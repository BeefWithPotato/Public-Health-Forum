{
    "name": "team08_server",
    "version": "0.1.0",
    "private": true,
    "scripts": {
        "setup": "npm install && cd client && npm install && cd ..",
        "build-run": "cd client && npm run build && cd .. && node ./bin/www",
        "start": "node ./bin/www",
        "heroku-postbuild": "cd client && npm install && npm run build",
    },
    "dependencies": {
        "bcrypt": "^5.0.0",
        "body-parser": "^1.19.0",
        "cookie-parser": "~1.4.4",
        "debug": "^4.1.1",
        "express": "^4.17.1",
        "express-session": "^1.17.1",
        "mongodb": "^3.6.0",
        "mongoose": "^5.10.0",
        "morgan": "^1.10.0",
        "validator": "^13.1.1"
    },
    "optionalDependencies": {
        "fsevents": "^2.1.3"
    }
}
